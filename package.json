{
  "name": "recoil",
  "version": "0.0.0",
  "description": "Mock properties on global objects across a `require` call.",
  "author": "Lim Yuan Qing",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/yuanqing/recoil.git"
  },
  "dependencies": {
    "callsite": "^1.0.0",
    "lodash.foreach": "^2.4.1",
    "require-uncached": "^1.0.2"
  },
  "devDependencies": {
    "istanbul": "^0.3.5",
    "jshint": "^2.5.11",
    "tape": "^3.0.3"
  },
  "scripts": {
    "lint": "node_modules/.bin/jshint --verbose index.js test/*.js",
    "test": "rm -rf coverage && node_modules/.bin/istanbul cover tape test/*.js"
  },
  "keywords": [
    "global",
    "globals",
    "mock",
    "polyfill",
    "properties",
    "property",
    "require",
    "stub",
    "test"
  ]
}
